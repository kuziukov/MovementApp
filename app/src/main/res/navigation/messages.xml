<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/messages"
    app:startDestination="@+id/navigation_notifications"
    tools:ignore="UnusedNavigation">

    <fragment
        android:id="@+id/navigation_notifications"
        android:name="com.example.movementapp.ui.chats.ChatsFragment"
        android:label="@string/title_notifications"
        tools:layout="@layout/fragment_notifications" >
        <action
            android:id="@+id/action_navigation_notifications_to_chatFragment"
            app:destination="@id/chatFragment"
            app:enterAnim="@animator/slide_left_in"
            app:exitAnim="@animator/slide_left_out"
            app:launchSingleTop="true"
            app:popEnterAnim="@animator/slide_right_in"
            app:popExitAnim="@animator/slide_right_out" />
    </fragment>

    <fragment
        android:id="@+id/chatFragment"
        android:name="com.example.movementapp.ui.messages.MessagesFragment"
        android:label="fragment_chat"
        tools:layout="@layout/fragment_chat" />

</navigation>