<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/list_item_message_sent"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="6dp"
    android:paddingStart="8dp"
    android:paddingLeft="8dp"
    android:paddingTop="2dp"
    android:paddingEnd="60dp"
    android:paddingRight="60dp">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/icon"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:layout_marginEnd="6dp"
        android:layout_marginRight="6dp"
        android:src="@drawable/ic_no_avatar"
        app:civ_border_color="#FF000000"
        app:civ_border_width="0dp" />

    <TextView
        android:id="@+id/message_body"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@+id/icon"
        android:layout_toRightOf="@+id/icon"
        android:background="@drawable/message_received"
        android:maxWidth="350dp"
        android:minWidth="70dp"
        android:paddingLeft="13dp"
        android:paddingTop="9dp"
        android:paddingRight="13dp"
        android:paddingBottom="9dp"
        android:text="Somehow this layout is not working for me. All I see is a white little round corner rectangle at the top left corner and a bigger grey arrow is seen overlapped. Any idea what am I missing?"
        android:textColor="@color/colorWhite"
        android:textSize="16dp" />

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/message_body"
        android:layout_alignEnd="@+id/message_body"
        android:layout_alignRight="@+id/message_body"
        android:layout_marginStart="16dp"
        android:layout_marginLeft="16dp"
        android:paddingTop="4dp">

        <TextView
            android:id="@+id/messageTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:text="10:40 PM"
            android:textSize="11dp" />

    </RelativeLayout>


</RelativeLayout>